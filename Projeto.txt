-- Criar Banco
CREATE DATABASE	banco;

-- Remover um Banco
DROP DATABASE teste;

-- Exibir o banco de dados
SHOW DATABASES;

-- CRIAR = CREATE
-- DELETAR = DROP

CREATE TABLE minhatabela(
	nome VARCHAR(100)
);
 
 -- REMOVER
DROP TABLE minhatabela 

-- 	TIPOS DE DADOS
-- TEXTO = VARCHAR(X)
-- NUMERO = INT (X)alter
-- DATAS = DATE

CREATE TABLE pessoas (
	nome  varchar (100)
)

CREATE TABLE pessoas (
	nome  varchar (100)
)


///////////
Video aula: 19/05

CREATE marcas (
	id INTEGER PRIMARY KEY AUTO_INCREMENT,
  nome VARCHAR(100) NOT NULL UNIQUE,	
	site VARCHAR(100), 	
	telefone VARCHAR (15),
)

CRETAE TABLE produtos (
	id INTEGER PRIMARY KEY AUTO_INCREMENT, 
	nome VARCHAR(100) NOT NULL, 
	preco REAL, 
	estoque INTEGER DEFAULT 0, 
	idMarca INTEGER FOREING KEY REFERENCES
)